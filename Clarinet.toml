[project]
name = "network-monitor"
authors = ["DevOps Team"]
description = "A decentralized network monitoring and health tracking system"
telemetry = false
requirements = []
[contracts.network-monitor]
path = "contracts/network-monitor.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
